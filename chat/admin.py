from django.contrib import admin
# Remove or comment out the old model registrations since we're not using them anymore
# from .models import Room, Message

# admin.site.register(Room)
# admin.site.register(Message)